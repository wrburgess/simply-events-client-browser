import React, { Component } from "react";
import styles from "../stylesheets/footer.scss";

const Footer = () => {
  return(
    <footer>
      This is the footer
    </footer>
  );
}

export default Footer;
