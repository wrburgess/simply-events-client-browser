import React, { Component } from "react";
import styles from "../stylesheets/header.scss";

const Header = () => {
  return(
    <header>
      This is the header
    </header>
  );
}

export default Header;
